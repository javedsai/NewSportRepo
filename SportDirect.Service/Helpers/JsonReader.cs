using System;
using System.Collections.Generic;
using System.IO;
using System.Text;
using System.Threading.Tasks;

namespace SportDirect.Service.Helpers
{
   public class JsonReader
    {
        //public static async Task<string> ReadJsonString(string jsonName)
        //{
        //    var assembly = typeof(LoginResponseModel).GetTypeInfo().Assembly;
        //    Stream stream = assembly.GetManifestResourceStream("Shopify.Data.FakeData." + jsonName);

        //    using (var reader = new System.IO.StreamReader(stream))
        //    {

        //        var json = reader.ReadToEnd();
        //        // var data = JsonConvert.DeserializeObject<T>(json, new JsonSerializerSettings { Culture = CultureInfo.InvariantCulture });
        //        return json;
        //    }
        //}
    }
}
